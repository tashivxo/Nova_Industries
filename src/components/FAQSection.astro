---
import { faqs } from '../data/faqs';
---

<section class="faq section-pad" id="faq">
  <div class="faq-head">
    <h2 class="font-display">FAQ</h2>
    <p class="eyebrow">Clear Answers. No Guesswork.</p>
  </div>
  <div class="faq-list">
    {
      faqs.map((item) => (
        <details class="faq-item">
          <summary>{item.question}</summary>
          <p>{item.answer}</p>
        </details>
      ))
    }
  </div>
</section>

<style>
  .faq {
    padding-block: 5rem 3rem;
    border-top: 1px solid var(--line);
  }

  .faq-head {
    display: flex;
    flex-wrap: wrap;
    align-items: baseline;
    justify-content: space-between;
    gap: 1rem;
    margin-bottom: 2rem;
  }

  .faq-head h2 {
    font-size: clamp(2.5rem, 8vw, 5rem);
    margin: 0;
    line-height: 0.9;
  }

  .faq-item {
    border-top: 1px solid var(--line);
  }

  .faq-item:last-child {
    border-bottom: 1px solid var(--line);
  }

  summary {
    list-style: none;
    cursor: pointer;
    padding: 1.25rem 0;
    font-size: 0.95rem;
    font-weight: 500;
  }

  summary::-webkit-details-marker {
    display: none;
  }

  .faq-item p {
    margin: 0 0 1.25rem;
    max-width: 42rem;
    color: var(--muted);
    line-height: 1.6;
    font-size: 0.95rem;
  }
</style>
