---
import { navItems, brand } from '../data/site';

interface Props {
  activePath?: string;
}

const { activePath = '/' } = Astro.props;

function isActive(href: string, path: string) {
  if (href === '/') return path === '/';
  return path === href || path.startsWith(href + '/');
}
---

<header class="site-header">
  <nav class="site-nav" aria-label="Primary">
    {
      navItems.map((item) => (
        <a
          href={item.href}
          class:list={['nav-link', { active: isActive(item.href, activePath) }]}
        >
          {item.label}
        </a>
      ))
    }
  </nav>

  <a href="/" class="wordmark" aria-label={brand.name}>
    <span class="mark" aria-hidden="true">
      <span></span><span></span><span></span><span></span>
    </span>
    <span class="wordmark-text">{brand.name}</span>
  </a>

  <a href="/contact" class="contact-cta">
    <span class="contact-arrow" aria-hidden="true">↗</span>
    Contact
  </a>

  <button type="button" class="mobile-toggle" aria-label="Open menu" data-menu-toggle>
    <span></span><span></span><span></span>
  </button>
</header>

<div class="mobile-drawer" data-menu-drawer hidden>
  <nav aria-label="Mobile">
    {
      navItems.map((item) => (
        <a href={item.href} class:list={[{ active: isActive(item.href, activePath) }]}>
          {item.label}
        </a>
      ))
    }
    <a href="/contact">Contact</a>
  </nav>
</div>

<style>
  .site-header {
    position: fixed;
    inset: 0 0 auto 0;
    z-index: 50;
    display: grid;
    grid-template-columns: 1fr auto 1fr;
    align-items: start;
    padding: 1.75rem var(--page-x) 0;
    pointer-events: none;
  }

  .site-header > * {
    pointer-events: auto;
  }

  .site-nav {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
    align-items: flex-start;
  }

  .nav-link {
    font-size: 0.875rem;
    font-weight: 500;
    letter-spacing: 0;
    line-height: 1.25;
    text-transform: uppercase;
    color: var(--nav);
    transition: color 0.2s ease;
  }

  .nav-link:hover,
  .nav-link.active {
    color: #fff;
  }

  .wordmark {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    justify-self: center;
    margin-top: 0.1rem;
  }

  .wordmark-text {
    font-family: var(--font-sans);
    font-size: 0.875rem;
    font-weight: 500;
    letter-spacing: 0.02em;
    text-transform: uppercase;
    white-space: nowrap;
  }

  .contact-cta {
    justify-self: end;
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    margin-top: 0.05rem;
    font-size: 0.875rem;
    font-weight: 500;
    letter-spacing: 0;
    text-transform: uppercase;
  }

  .contact-arrow {
    color: var(--accent);
    font-size: 0.95rem;
    line-height: 1;
    transform: translateY(-0.02em);
  }

  .mobile-toggle {
    display: none;
    justify-self: end;
    width: 1.7rem;
    height: 1.15rem;
    border: 0;
    background: transparent;
    padding: 0;
    cursor: pointer;
  }

  .mobile-toggle span {
    display: block;
    height: 1.5px;
    background: #fff;
    margin: 0.28rem 0;
  }

  .mobile-drawer {
    position: fixed;
    inset: 0;
    z-index: 60;
    background: rgba(13, 13, 13, 0.98);
    padding: 5.5rem var(--page-x) 2rem;
  }

  .mobile-drawer nav {
    display: flex;
    flex-direction: column;
    gap: 1.15rem;
  }

  .mobile-drawer a {
    font-size: 1.15rem;
    font-weight: 500;
    letter-spacing: 0;
    text-transform: uppercase;
    color: var(--nav);
  }

  .mobile-drawer a.active,
  .mobile-drawer a:hover {
    color: #fff;
  }

  @media (max-width: 900px) {
    .site-nav,
    .contact-cta {
      display: none;
    }

    .site-header {
      grid-template-columns: 1fr auto;
      align-items: center;
      padding-block: 1.15rem 0;
    }

    .wordmark {
      justify-self: start;
      margin-top: 0;
    }

    .wordmark-text {
      font-size: 0.78rem;
      letter-spacing: 0.04em;
    }

    .mobile-toggle {
      display: block;
    }
  }
</style>

<script>
  const toggle = document.querySelector('[data-menu-toggle]');
  const drawer = document.querySelector('[data-menu-drawer]');
  toggle?.addEventListener('click', () => {
    const open = !drawer?.hasAttribute('hidden');
    if (open) drawer?.setAttribute('hidden', '');
    else drawer?.removeAttribute('hidden');
  });
</script>
